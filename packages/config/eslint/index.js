const base = require('./src/base');
const next = require('./src/next');
const recommended = require('./src/recommended');

module.exports = {
  configs: {
    base,
    next,
    recommended,
  },
};
